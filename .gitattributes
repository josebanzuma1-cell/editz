# Normalise to LF in the repository. Half the team will be on Windows and the
# workers run on Linux; without this, line endings churn in every diff.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
*.woff2 binary
*.mp4 binary
